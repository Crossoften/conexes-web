diff --git a/src/app/features/cost-centers/cost-centers-list.page.html b/src/app/features/cost-centers/cost-centers-list.page.html
index caeaaa6..a84276b 100644
--- a/src/app/features/cost-centers/cost-centers-list.page.html
+++ b/src/app/features/cost-centers/cost-centers-list.page.html
@@ -104,7 +104,7 @@
               <tr class="tr" [class.tr--expanded]="store.expandedIds().has(toStr(item.id))">
 
                 <td class="td td--expand">
-                  @if (item.groups?.length) {
+                  @if (item._children?.length) {
                     <button class="expand-btn"
                       [class.expand-btn--open]="store.expandedIds().has(toStr(item.id))"
                       (click)="store.toggleExpand(toStr(item.id))"
@@ -157,27 +157,42 @@
                 </td>
               </tr>
 
-              @if (store.expandedIds().has(toStr(item.id)) && item.groups?.length) {
-                <tr class="tr-expand-panel">
-                  <td colspan="8" class="td-expand-panel">
-                    @for (group of item.groups; track group.id) {
-                      <div class="expand-group">
-                        <div class="expand-group-header">
-                          <span class="expand-group-code">{{ group.code }} – {{ group.name }}</span>
-                          <span class="expand-group-col">Descrição</span>
-                          <span class="expand-group-col">Status</span>
-                        </div>
-                        @for (child of group.children; track child.id) {
-                          <div class="expand-group-row">
-                            <span class="expand-child-title">{{ child.code }} – {{ child.name }}</span>
-                            <span class="expand-child-desc">{{ child.description }}</span>
-                            <span class="expand-child-status">{{ child.status === 'Active' ? 'Ativo' : 'Inativo' }}</span>
-                          </div>
+              @if (store.expandedIds().has(toStr(item.id))) {
+                @for (child of item._children ?? []; track child.id) {
+                  <tr class="tr tr--child">
+                    <td class="td td--expand"></td>
+                    <td class="td td--mono td--child-first">{{ child.code }}</td>
+                    <td class="td td--name">{{ child.name || child.title }}</td>
+                    <td class="td">{{ typeLabel(child.type) }}</td>
+                    <td class="td">{{ child.restrictBudget ? 'Sim' : 'Não' }}</td>
+                    <td class="td">{{ child.description }}</td>
+
+                    <td class="td">
+                      <span class="status-badge" [ngClass]="'status-badge--' + statusConfig[child.status].variant">
+                        @if (child.status === 'Active') {
+                          <svg viewBox="0 0 14 14" fill="none" width="13" height="13"><circle cx="7" cy="7" r="5.5" stroke="currentColor" stroke-width="1.3"/><path d="M4.5 7l2 2 3-3" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/></svg>
+                        } @else {
+                          <svg viewBox="0 0 14 14" fill="none" width="13" height="13"><circle cx="7" cy="7" r="5.5" stroke="currentColor" stroke-width="1.3"/><path d="M9 5L5 9M5 5l4 4" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/></svg>
                         }
+                        {{ statusConfig[child.status].label }}
+                      </span>
+                    </td>
+
+                    <td class="td td--center">
+                      <div class="action-icons">
+                        <button class="icon-btn" title="Visualizar" (click)="onView(child)">
+                          <svg viewBox="0 0 20 20" fill="none" width="16" height="16"><path d="M1 10s3.5-6 9-6 9 6 9 6-3.5 6-9 6-9-6-9-6z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/><circle cx="10" cy="10" r="2.5" stroke="currentColor" stroke-width="1.5"/></svg>
+                        </button>
+                        <button class="icon-btn" title="Editar" (click)="onEdit(child)">
+                          <svg viewBox="0 0 20 20" fill="none" width="16" height="16"><path d="M13.5 3.5a2.121 2.121 0 0 1 3 3L7 16H4v-3L13.5 3.5z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
+                        </button>
+                        <button class="icon-btn icon-btn--danger" title="Excluir" (click)="onDelete(child)">
+                          <svg viewBox="0 0 20 20" fill="none" width="16" height="16"><path d="M4 5h12M6 5V3c0-1.1.9-2 2-2h4c1.1 0 2 .9 2 2v2M7 5v12c0 1.1.9 2 2 2h2c1.1 0 2-.9 2-2V5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
+                        </button>
                       </div>
-                    }
-                  </td>
-                </tr>
+                    </td>
+                  </tr>
+                }
               }
 
             }
diff --git a/src/app/features/cost-centers/cost-centers-list.page.scss b/src/app/features/cost-centers/cost-centers-list.page.scss
index 892a729..f88dcf6 100644
--- a/src/app/features/cost-centers/cost-centers-list.page.scss
+++ b/src/app/features/cost-centers/cost-centers-list.page.scss
@@ -208,6 +208,27 @@
   &--empty  { padding: 64px 16px; text-align: center; }
 }
 
+// ── Linhas de subnível (projetos aninhados sob o CC) ──
+.tr--child {
+  background: #FBFAFF;
+  &:hover { background: #F5F3FF; }
+}
+
+.td--child-first {
+  padding-left: 44px;
+  position: relative;
+
+  &::before {
+    content: '';
+    position: absolute;
+    left: 24px;
+    top: 50%;
+    width: 12px;
+    height: 1px;
+    background: var(--brand-purple-mid, #C4B5FD);
+  }
+}
+
 .empty-title { font-size: 15px; font-weight: 600; color: var(--text-primary); margin-bottom: 4px; }
 .empty-desc  { font-size: 13px; color: var(--text-muted); }
 
diff --git a/src/app/features/cost-centers/cost-centers.model.ts b/src/app/features/cost-centers/cost-centers.model.ts
index 78ab99e..b6da587 100644
--- a/src/app/features/cost-centers/cost-centers.model.ts
+++ b/src/app/features/cost-centers/cost-centers.model.ts
@@ -44,6 +44,7 @@ export interface CostCenter {
   costCenterId:         number | null;
   linkedAccounts:       LinkedAccount[];
   groups?:              CostCenterGroup[];
+  _children?:           CostCenter[]; // subníveis (projetos) montados no Front para a expansão
   createdAt?:           string;
   updatedAt?:           string;
 }
diff --git a/src/app/features/cost-centers/cost-centers.store.ts b/src/app/features/cost-centers/cost-centers.store.ts
index 31ec54f..6c28507 100644
--- a/src/app/features/cost-centers/cost-centers.store.ts
+++ b/src/app/features/cost-centers/cost-centers.store.ts
@@ -1,7 +1,7 @@
 // src/app/features/cost-centers/cost-centers.store.ts
 import { signalStore, withState, withComputed, withMethods, patchState } from '@ngrx/signals';
 import { inject, computed } from '@angular/core';
-import { CostCenter, CostCenterStatus, CostCenterGroup, resolveEntityType } from './cost-centers.model';
+import { CostCenter, CostCenterStatus, resolveEntityType } from './cost-centers.model';
 import { CostCentersService } from './cost-centers.service';
 
 export type SortDirection = 'asc' | 'desc' | null;
@@ -73,26 +73,26 @@ export const CostCentersStore = signalStore(
       const all     = sortedItems();
       const parents = all.filter(i => resolveEntityType(i) === 'cost_center');
       const parentIds = new Set(parents.map(p => p.id));
-      const groupsByParent = new Map<number, CostCenterGroup[]>();
+      const childrenByParent = new Map<number, CostCenter[]>();
       const orphans: CostCenter[] = [];
 
       for (const i of all) {
         if (resolveEntityType(i) === 'cost_center') continue;
         if (i.costCenterId != null && parentIds.has(i.costCenterId)) {
-          const arr = groupsByParent.get(i.costCenterId) ?? [];
-          arr.push({ id: i.id, code: i.code, name: i.name || i.title || '', children: [] });
-          groupsByParent.set(i.costCenterId, arr);
+          const arr = childrenByParent.get(i.costCenterId) ?? [];
+          arr.push(i);
+          childrenByParent.set(i.costCenterId, arr);
         } else {
           orphans.push(i);
         }
       }
 
-      const withGroups = parents.map(p => ({
+      const withChildren = parents.map(p => ({
         ...p,
-        groups: groupsByParent.get(p.id) ?? p.groups ?? [],
+        _children: childrenByParent.get(p.id) ?? [],
       }));
 
-      return [...withGroups, ...orphans];
+      return [...withChildren, ...orphans];
     });
 
     return { sortedItems, treeItems, totalPages, expandedIds };
