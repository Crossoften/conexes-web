diff --git a/src/app/features/cost-centers/cost-centers-list.page.html b/src/app/features/cost-centers/cost-centers-list.page.html
index 1d5b2ff..43bd192 100644
--- a/src/app/features/cost-centers/cost-centers-list.page.html
+++ b/src/app/features/cost-centers/cost-centers-list.page.html
@@ -143,6 +143,13 @@
                     <button class="icon-btn" title="Editar" (click)="onEdit(item)">
                       <svg viewBox="0 0 20 20" fill="none" width="16" height="16"><path d="M13.5 3.5a2.121 2.121 0 0 1 3 3L7 16H4v-3L13.5 3.5z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
                     </button>
+                    @if (item.type === 'centro_de_custo') {
+                      <a class="icon-btn" title="Adicionar subnível (projeto)"
+                        [routerLink]="['/cost-centers/new']"
+                        [queryParams]="{ type: 'projeto', parentId: item.id, parentName: item.name || item.title }">
+                        <svg viewBox="0 0 20 20" fill="none" width="16" height="16"><path d="M10 5v10M5 10h10" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>
+                      </a>
+                    }
                     <button class="icon-btn icon-btn--danger" title="Excluir" (click)="onDelete(item)">
                       <svg viewBox="0 0 20 20" fill="none" width="16" height="16"><path d="M4 5h12M6 5V3c0-1.1.9-2 2-2h4c1.1 0 2 .9 2 2v2M7 5v12c0 1.1.9 2 2 2h2c1.1 0 2-.9 2-2V5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
                     </button>
diff --git a/src/app/features/cost-centers/new/cost-centers-new.page.html b/src/app/features/cost-centers/new/cost-centers-new.page.html
index 9752392..fcc48f1 100644
--- a/src/app/features/cost-centers/new/cost-centers-new.page.html
+++ b/src/app/features/cost-centers/new/cost-centers-new.page.html
@@ -18,6 +18,12 @@
           <div class="error-banner" role="alert">{{ errorMsg() }}</div>
         }
 
+        @if (parentName()) {
+          <div class="parent-hint">
+            Novo subnível vinculado a: <strong>{{ parentName() }}</strong>
+          </div>
+        }
+
         <div class="form-grid">
 
           <div class="form-row grid-cols-3">
diff --git a/src/app/features/cost-centers/new/cost-centers-new.page.scss b/src/app/features/cost-centers/new/cost-centers-new.page.scss
index cf53b15..daf33b4 100644
--- a/src/app/features/cost-centers/new/cost-centers-new.page.scss
+++ b/src/app/features/cost-centers/new/cost-centers-new.page.scss
@@ -1,6 +1,19 @@
 // src/app/features/cost-centers/new/cost-centers-new.page.scss
 :host { display: block; }
 
+// Aviso de vínculo com o pai (fluxo "Adicionar Subnível")
+.parent-hint {
+  margin-bottom: 16px;
+  padding: 10px 14px;
+  border-radius: 8px;
+  background: #F3F0FF;
+  border: 1px solid #DDD6FE;
+  color: #5B21B6;
+  font-size: 13px;
+
+  strong { font-weight: 700; }
+}
+
 // ── Tipografia e Cabeçalho ────────────────────
 .page-title {
   font-family: var(--font-display);
diff --git a/src/app/features/cost-centers/new/cost-centers-new.page.ts b/src/app/features/cost-centers/new/cost-centers-new.page.ts
index 2b0e1b9..a92dad3 100644
--- a/src/app/features/cost-centers/new/cost-centers-new.page.ts
+++ b/src/app/features/cost-centers/new/cost-centers-new.page.ts
@@ -1,6 +1,6 @@
 // src/app/features/cost-centers/new/cost-centers-new.page.ts
 import { Component, inject, signal, OnInit } from '@angular/core';
-import { Router, RouterLink } from '@angular/router';
+import { Router, RouterLink, ActivatedRoute } from '@angular/router';
 import { ReactiveFormsModule, FormsModule, FormBuilder, FormGroup, Validators } from '@angular/forms';
 import { NgClass } from '@angular/common';
 import { CostCentersService } from '../cost-centers.service';
@@ -30,6 +30,7 @@ interface EntityItem {
 export class CostCentersNewPage implements OnInit {
   private fb     = inject(FormBuilder);
   private router = inject(Router);
+  private route  = inject(ActivatedRoute);
   private svc    = inject(CostCentersService);
   private http   = inject(HttpClient);
 
@@ -39,6 +40,9 @@ export class CostCentersNewPage implements OnInit {
   readonly entities     = signal<EntityItem[]>([]);
   readonly loadingLists = signal(true);
 
+  // Preenchido quando a tela é aberta via "Adicionar Subnível" a partir de um pai.
+  readonly parentName = signal<string | null>(null);
+
   // Contas vinculadas gerenciadas separadamente (array dinâmico)
   linkedAccounts: LinkedAccount[] = [];
   selectedLinkedAccount = '';
@@ -60,6 +64,17 @@ export class CostCentersNewPage implements OnInit {
   // ── Lifecycle ─────────────────────────────────────────────────────────────
 
   ngOnInit(): void {
+    // "Adicionar Subnível": abre já vinculado a um pai (ex.: projeto dentro de um CC).
+    const qp       = this.route.snapshot.queryParamMap;
+    const parentId = qp.get('parentId');
+    if (parentId) {
+      this.form.patchValue({
+        projectType:  qp.get('type') ?? 'projeto',
+        costCenterId: Number(parentId),
+      });
+      this.parentName.set(qp.get('parentName'));
+    }
+
     let loaded = 0;
     const checkDone = () => { if (++loaded >= 2) this.loadingLists.set(false); };
 
