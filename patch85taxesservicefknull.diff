diff --git a/src/app/features/taxes/components/tax-detail.modal.ts b/src/app/features/taxes/components/tax-detail.modal.ts
index 4510694..4ae577b 100644
--- a/src/app/features/taxes/components/tax-detail.modal.ts
+++ b/src/app/features/taxes/components/tax-detail.modal.ts
@@ -160,7 +160,7 @@ export class TaxDetailModalComponent implements OnChanges {
       cbsAliquot:       Number(v.aliqCBS)              || 0,
       cbsCode:          v.cbsCode                      ?? '',
       status:           this.tax.status ?? 'Active',
-      services:         this.services,
+      services:         this.normalizeServices(this.services),
     };
 
     this.save.emit(payload);
@@ -193,7 +193,22 @@ export class TaxDetailModalComponent implements OnChanges {
       name: '', description: '', externalCode: '',
       grantorOrgan: '', hasRetention: false,
       accessorOrgan: '', concessionLink: '',
-      costCenterId: 0, projectId: 0, activityId: 0,
+      costCenterId: null, projectId: null, activityId: null,
     };
   }
+
+  /** FK do serviço: id positivo ou null (o back rejeita 0/"" como referência inexistente). */
+  private idOrNull(v: unknown): number | null {
+    const n = Number(v);
+    return Number.isFinite(n) && n > 0 ? n : null;
+  }
+
+  private normalizeServices(list: TaxService[]): TaxService[] {
+    return list.map(s => ({
+      ...s,
+      costCenterId: this.idOrNull(s.costCenterId),
+      projectId:    this.idOrNull(s.projectId),
+      activityId:   this.idOrNull(s.activityId),
+    }));
+  }
 }
diff --git a/src/app/features/taxes/new/taxes-new.page.ts b/src/app/features/taxes/new/taxes-new.page.ts
index ab9f086..652b0e8 100644
--- a/src/app/features/taxes/new/taxes-new.page.ts
+++ b/src/app/features/taxes/new/taxes-new.page.ts
@@ -38,10 +38,25 @@ export class TaxesNewPage implements OnInit {
       name: '', description: '', externalCode: '',
       grantorOrgan: '', hasRetention: false,
       accessorOrgan: '', concessionLink: '',
-      costCenterId: 0, projectId: 0, activityId: 0,
+      costCenterId: null, projectId: null, activityId: null,
     };
   }
 
+  /** FK do serviço: id positivo ou null (o back rejeita 0/"" como referência inexistente). */
+  private idOrNull(v: unknown): number | null {
+    const n = Number(v);
+    return Number.isFinite(n) && n > 0 ? n : null;
+  }
+
+  private normalizeServices(list: TaxService[]): TaxService[] {
+    return list.map(s => ({
+      ...s,
+      costCenterId: this.idOrNull(s.costCenterId),
+      projectId:    this.idOrNull(s.projectId),
+      activityId:   this.idOrNull(s.activityId),
+    }));
+  }
+
   form: FormGroup = this.fb.group({
     fornecedor:              ['', Validators.required],
     codigoServico:           ['', Validators.required],
@@ -204,7 +219,7 @@ export class TaxesNewPage implements OnInit {
       cbsAliquot:      Number(v.aliqCBS)               || 0,
       cbsCode:         v.cbsCode                       ?? '',
       status:          'Active',
-      services:        this.services,
+      services:        this.normalizeServices(this.services),
     };
 
     this.svc.save(payload).subscribe({
diff --git a/src/app/features/taxes/taxes.model.ts b/src/app/features/taxes/taxes.model.ts
index 53b3400..0331b9a 100644
--- a/src/app/features/taxes/taxes.model.ts
+++ b/src/app/features/taxes/taxes.model.ts
@@ -12,9 +12,9 @@ export interface TaxService {
   hasRetention:   boolean;
   accessorOrgan:  string;
   concessionLink: string;
-  costCenterId:   number;
-  projectId:      number;
-  activityId:     number;   // Atividade (3º nível) vinculada, além do Projeto
+  costCenterId:   number | null;
+  projectId:      number | null;
+  activityId:     number | null;   // Atividade (3º nível) vinculada, além do Projeto
 }
 
 // ── Model completo (resposta da API) ──────────────────────────────────────────
