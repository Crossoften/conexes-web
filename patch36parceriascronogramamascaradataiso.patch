diff --git a/src/app/features/contract-transfers/new/contract-transfer-new.page.html b/src/app/features/contract-transfers/new/contract-transfer-new.page.html
index 6a1bd9a..8d75584 100644
--- a/src/app/features/contract-transfers/new/contract-transfer-new.page.html
+++ b/src/app/features/contract-transfers/new/contract-transfer-new.page.html
@@ -219,7 +219,8 @@
                     </div>
                   </div>
                   <div class="form-group">
-                    <input type="text" class="form-control" placeholder="00/00/0000" formControlName="dueDate">
+                    <input type="text" class="form-control" placeholder="dd/mm/aaaa" inputmode="numeric" maxlength="10"
+                      formControlName="dueDate" (input)="applyDateMask($event, row.get('dueDate'))">
                   </div>
                   <div class="form-group" style="display:flex; gap:8px; align-items:flex-end;">
                     <div style="flex:1;">
diff --git a/src/app/features/contract-transfers/new/contract-transfer-new.page.ts b/src/app/features/contract-transfers/new/contract-transfer-new.page.ts
index ff9bf11..7c5f077 100644
--- a/src/app/features/contract-transfers/new/contract-transfer-new.page.ts
+++ b/src/app/features/contract-transfers/new/contract-transfer-new.page.ts
@@ -1,10 +1,11 @@
 // src/app/features/contract-transfers/new/contract-transfer-new.page.ts
 import { Component, OnInit, inject, signal } from '@angular/core';
 import { Router, RouterLink, ActivatedRoute } from '@angular/router';
-import { ReactiveFormsModule, FormBuilder, FormGroup, FormArray, Validators } from '@angular/forms';
+import { ReactiveFormsModule, FormBuilder, FormGroup, FormArray, AbstractControl, Validators } from '@angular/forms';
 import { ContractTransfersService } from '../contract-transfers.service';
 import {
   PartnershipPayload,
+  PartnershipPayablePayload,
   PartnershipRef,
   PartnershipDetail,
   PartnershipResponsiblePayload,
@@ -166,6 +167,33 @@ export class ContractTransferNewPage implements OnInit {
   private dstr(s: string | null | undefined): string { return s ? String(s).slice(0, 10) : ''; }
   private nstr(n: number | null | undefined): string { return n != null ? String(n) : ''; }
 
+  /** ISO (yyyy-mm-dd…) → dd/mm/aaaa para exibição nos campos mascarados. */
+  private dbr(value: string | null | undefined): string {
+    if (!value) return '';
+    const m = String(value).slice(0, 10).match(/^(\d{4})-(\d{2})-(\d{2})$/);
+    return m ? `${m[3]}/${m[2]}/${m[1]}` : '';
+  }
+
+  /** dd/mm/aaaa → ISO 8601 (UTC). Vazio/ inválido → undefined. */
+  private toIso(value: string | null | undefined): string | undefined {
+    if (!value) return undefined;
+    const m = String(value).match(/^(\d{2})\/(\d{2})\/(\d{4})$/);
+    if (!m) return undefined;
+    const d = new Date(Date.UTC(Number(m[3]), Number(m[2]) - 1, Number(m[1])));
+    return isNaN(d.getTime()) ? undefined : d.toISOString();
+  }
+
+  /** Máscara dd/mm/aaaa aplicada ao control informado (linhas do cronograma). */
+  applyDateMask(event: Event, control: AbstractControl | null): void {
+    const input  = event.target as HTMLInputElement;
+    const digits = input.value.replace(/\D/g, '').slice(0, 8);
+    let masked = digits;
+    if (digits.length > 4)      masked = `${digits.slice(0, 2)}/${digits.slice(2, 4)}/${digits.slice(4)}`;
+    else if (digits.length > 2) masked = `${digits.slice(0, 2)}/${digits.slice(2)}`;
+    input.value = masked;
+    control?.setValue(masked, { emitEvent: false });
+  }
+
   private hydrate(d: PartnershipDetail): void {
     const resp = d.responsibles ?? [];
     const anx  = d.annexes ?? [];
@@ -218,7 +246,7 @@ export class ContractTransferNewPage implements OnInit {
         this.payables.push(this.newPayable(
           p.installment ?? i + 1,
           p.competency ?? '',
-          this.dstr(p.dueDate),
+          this.dbr(p.dueDate),
           this.nstr(p.value),
         ));
       });
@@ -318,14 +346,16 @@ export class ContractTransferNewPage implements OnInit {
     if (a1.printDate || a1.deadlineDate || a1.validationType) annexes.push(a1);
     if (a2.printDate || a2.deadlineDate || a2.validationType) annexes.push(a2);
 
+    // O back exige dueDate obrigatório e em ISO 8601; convertemos dd/mm/aaaa → ISO
+    // e descartamos linhas sem data válida (não há repasse sem data prevista).
     const payables = (v.payables as { installment: number; competency: string; dueDate: string; value: string }[])
-      .filter(p => p.competency || p.dueDate || p.value)
       .map((p, i) => ({
         installment: Number(p.installment) || i + 1,
         competency:  p.competency || undefined,
-        dueDate:     p.dueDate,
+        dueDate:     this.toIso(p.dueDate),
         value:       toNumber(p.value) ?? 0,
-      }));
+      }))
+      .filter((p): p is PartnershipPayablePayload => !!p.dueDate);
 
     const payload: PartnershipPayload = {
       title:                 v.title,
