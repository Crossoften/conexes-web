diff --git a/src/app/features/entity-registry/components/entity-registry-detail.modal.html b/src/app/features/entity-registry/components/entity-registry-detail.modal.html
index c9e486b..d43bde0 100644
--- a/src/app/features/entity-registry/components/entity-registry-detail.modal.html
+++ b/src/app/features/entity-registry/components/entity-registry-detail.modal.html
@@ -367,6 +367,16 @@
     </div>
     <!-- /Body -->
 
+    <!-- Aviso de validação (campos obrigatórios) -->
+    <div class="modal-required-warning" *ngIf="!loading && mode === 'edit' && showRequiredWarning" role="alert">
+      <svg viewBox="0 0 24 24" fill="none" width="18" height="18" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
+        <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"></path>
+        <line x1="12" y1="9" x2="12" y2="13"></line>
+        <line x1="12" y1="17" x2="12.01" y2="17"></line>
+      </svg>
+      Preencha todos os campos obrigatórios destacados antes de salvar.
+    </div>
+
     <!-- ── Footer (só aparece no modo edit) ───────────────────────────────── -->
     <div class="modal-footer" *ngIf="!loading && mode === 'edit'">
       <button type="button" class="btn-outline btn-outline--orange" (click)="onCancelEdit()">
diff --git a/src/app/features/entity-registry/components/entity-registry-detail.modal.scss b/src/app/features/entity-registry/components/entity-registry-detail.modal.scss
index 8848b3a..db9326e 100644
--- a/src/app/features/entity-registry/components/entity-registry-detail.modal.scss
+++ b/src/app/features/entity-registry/components/entity-registry-detail.modal.scss
@@ -333,6 +333,25 @@
   }
 }
 
+// ── Aviso de campos obrigatórios (edit) ───────
+
+.modal-required-warning {
+  display: flex;
+  align-items: center;
+  gap: 8px;
+  margin: 0 32px;
+  padding: 12px 16px;
+  border-radius: 8px;
+  background: rgba(239, 68, 68, 0.08);
+  border: 1px solid rgba(239, 68, 68, 0.3);
+  color: #B91C1C;
+  font-size: 13.5px;
+  font-weight: 500;
+  flex-shrink: 0;
+
+  svg { flex-shrink: 0; }
+}
+
 // ── Rodapé (edit) ─────────────────────────────
 
 .modal-footer {
diff --git a/src/app/features/entity-registry/components/entity-registry-detail.modal.ts b/src/app/features/entity-registry/components/entity-registry-detail.modal.ts
index 0bbd012..7a6d6f0 100644
--- a/src/app/features/entity-registry/components/entity-registry-detail.modal.ts
+++ b/src/app/features/entity-registry/components/entity-registry-detail.modal.ts
@@ -26,6 +26,7 @@ export class EntityRegistryDetailModalComponent implements OnChanges {
 
   protected mode: 'view' | 'edit' = 'view';
   protected activeTab: 'geral' | 'contador' = 'geral';
+  protected showRequiredWarning = false;
 
   protected readonly form = this.fb.group({
     cnpj:                  ['', Validators.required],
@@ -71,10 +72,12 @@ export class EntityRegistryDetailModalComponent implements OnChanges {
 
   onEdit(): void {
     this.mode = 'edit';
+    this.showRequiredWarning = false;
   }
 
   onCancelEdit(): void {
     this.mode = 'view';
+    this.showRequiredWarning = false;
     if (this.entity) this.form.patchValue(this.entity as any);
   }
 
@@ -85,9 +88,14 @@ export class EntityRegistryDetailModalComponent implements OnChanges {
   onSubmit(): void {
     if (this.form.invalid) {
       this.form.markAllAsTouched();
+      // Se algum campo obrigatório estiver em outra aba, leva o usuário até ela.
+      if (this.hasInvalidInTab('geral'))         this.activeTab = 'geral';
+      else if (this.hasInvalidInTab('contador')) this.activeTab = 'contador';
+      this.showRequiredWarning = true;
       return;
     }
 
+    this.showRequiredWarning = false;
     const raw = this.form.value;
     const onlyNumbers = (v: string | undefined) => v ? v.replace(/\D/g, '') : '';
 
@@ -105,4 +113,13 @@ export class EntityRegistryDetailModalComponent implements OnChanges {
 
     this.saved.emit(payload);
   }
+
+  /** True se houver campo inválido na aba informada (geral = entidade; contador = accountant*). */
+  private hasInvalidInTab(tab: 'geral' | 'contador'): boolean {
+    return Object.entries(this.form.controls).some(([name, ctrl]) => {
+      const isContador = name.startsWith('accountant');
+      const inTab = tab === 'contador' ? isContador : !isContador;
+      return inTab && ctrl.invalid;
+    });
+  }
 }
diff --git a/src/app/features/entity-registry/new/entity-registry-new.page.html b/src/app/features/entity-registry/new/entity-registry-new.page.html
index 6c743a4..a8c59ff 100644
--- a/src/app/features/entity-registry/new/entity-registry-new.page.html
+++ b/src/app/features/entity-registry/new/entity-registry-new.page.html
@@ -65,12 +65,15 @@
             </div>
             <div class="form-group">
               <label>Estado (UF)<span class="req">*</span></label>
-              <select class="form-control" formControlName="state">
-                <option value="">Selecione a UF</option>
-                @for (uf of ufOptions; track uf) {
-                  <option [value]="uf">{{ uf }}</option>
-                }
-              </select>
+              <div class="select-wrap">
+                <select class="form-control" formControlName="state">
+                  <option value="">Selecione a UF</option>
+                  @for (uf of ufOptions; track uf) {
+                    <option [value]="uf">{{ uf }}</option>
+                  }
+                </select>
+                <svg class="select-chevron" viewBox="0 0 16 16" fill="none"><path d="M4 6l4 4 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
+              </div>
             </div>
           </div>
 
diff --git a/src/app/features/entity-registry/new/entity-registry-new.page.scss b/src/app/features/entity-registry/new/entity-registry-new.page.scss
index 29fe5c1..0eb59f2 100644
--- a/src/app/features/entity-registry/new/entity-registry-new.page.scss
+++ b/src/app/features/entity-registry/new/entity-registry-new.page.scss
@@ -6,6 +6,31 @@
   border-radius: 24px !important; // Devolve o visual arredondado das caixas de texto
 }
 
+// ── Select alinhado ao padrão dos inputs (mesmo pill + chevron custom) ─────
+.select-wrap {
+  position: relative;
+
+  select.form-control {
+    width: 100%;
+    appearance: none;
+    -webkit-appearance: none;
+    -moz-appearance: none;
+    padding-right: 40px;
+    cursor: pointer;
+  }
+
+  .select-chevron {
+    position: absolute;
+    right: 16px;
+    top: 50%;
+    transform: translateY(-50%);
+    width: 16px;
+    height: 16px;
+    pointer-events: none;
+    color: var(--text-muted, #9CA3AF);
+  }
+}
+
 .btn-outline, .btn-solid, .tab-button {
   border-radius: 50px !important; // Garante os botões e abas em formato de pílula
 }
