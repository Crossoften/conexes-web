diff --git a/src/app/features/stakeholders/components/stakeholder-detail.modal.html b/src/app/features/stakeholders/components/stakeholder-detail.modal.html
index e2bbe2b..931271b 100644
--- a/src/app/features/stakeholders/components/stakeholder-detail.modal.html
+++ b/src/app/features/stakeholders/components/stakeholder-detail.modal.html
@@ -536,6 +536,10 @@
               </div>
 
               <h4 class="edit-subsection-title">Alíquotas</h4>
+              <label class="field-check" style="display:flex;align-items:center;gap:8px;margin-bottom:12px;">
+                <input type="checkbox" formControlName="manualAliquots">
+                <span class="field-label" style="margin:0;">Alíquotas manuais</span>
+              </label>
               <div class="edit-grid">
                 <div class="field">
                   <label class="field-label">IRRF — Alíquota (%)</label>
@@ -593,18 +597,42 @@
                   <label class="field-label">INSS — Alíquota (%)</label>
                   <input class="input" type="number" formControlName="inssAliquot">
                 </div>
+                <div class="field">
+                  <label class="field-label">INSS — Código</label>
+                  <input class="input" type="text" formControlName="inssCode">
+                </div>
                 <div class="field">
                   <label class="field-label">CSLL — Alíquota (%)</label>
                   <input class="input" type="number" formControlName="csllAliquot">
                 </div>
+                <div class="field">
+                  <label class="field-label">CSLL — Código</label>
+                  <input class="input" type="text" formControlName="csllCode">
+                </div>
+                <div class="field">
+                  <label class="field-label">ISS — Alíquota (%)</label>
+                  <input class="input" type="number" formControlName="issAliquot">
+                </div>
+                <div class="field">
+                  <label class="field-label">ISS — Código</label>
+                  <input class="input" type="text" formControlName="issCode">
+                </div>
                 <div class="field">
                   <label class="field-label">IBS — Alíquota (%)</label>
                   <input class="input" type="number" formControlName="ibsAliquot">
                 </div>
+                <div class="field">
+                  <label class="field-label">IBS — Código</label>
+                  <input class="input" type="text" formControlName="ibsCode">
+                </div>
                 <div class="field">
                   <label class="field-label">CBS — Alíquota (%)</label>
                   <input class="input" type="number" formControlName="cbsAliquot">
                 </div>
+                <div class="field">
+                  <label class="field-label">CBS — Código</label>
+                  <input class="input" type="text" formControlName="cbsCode">
+                </div>
               </div>
 
               <h4 class="edit-subsection-title">Serviços</h4>
diff --git a/src/app/features/stakeholders/components/stakeholder-detail.modal.ts b/src/app/features/stakeholders/components/stakeholder-detail.modal.ts
index f06cf76..97b9909 100644
--- a/src/app/features/stakeholders/components/stakeholder-detail.modal.ts
+++ b/src/app/features/stakeholders/components/stakeholder-detail.modal.ts
@@ -100,6 +100,7 @@ export class StakeholderDetailModalComponent implements OnChanges {
     serviceTitle:         [''],
     operationNature:      [''],
     totalRetentions:      [0],
+    manualAliquots:       [false],
     irfAliquot:           [0],
     irfCode:              [''],
     pisAliquot:           [0],
@@ -109,9 +110,15 @@ export class StakeholderDetailModalComponent implements OnChanges {
     cofinsAliquot:        [0],
     cofinsCode:           [''],
     inssAliquot:          [0],
+    inssCode:             [''],
     csllAliquot:          [0],
+    csllCode:             [''],
+    issAliquot:           [0],
+    issCode:              [''],
     ibsAliquot:           [0],
+    ibsCode:              [''],
     cbsAliquot:           [0],
+    cbsCode:              [''],
     serviceName:          [''],
     serviceDescription:   [''],
     serviceExternalCode:  [''],
@@ -196,6 +203,7 @@ export class StakeholderDetailModalComponent implements OnChanges {
       serviceTitle:         tax?.serviceTitle         ?? '',
       operationNature:      tax?.operationNature      ?? '',
       totalRetentions:      tax?.totalRetentions      ?? 0,
+      manualAliquots:       tax?.manualAliquots       ?? false,
       irfAliquot:           tax?.irfAliquot           ?? 0,
       irfCode:              tax?.irfCode              ?? '',
       pisAliquot:           tax?.pisAliquot           ?? 0,
@@ -205,9 +213,15 @@ export class StakeholderDetailModalComponent implements OnChanges {
       cofinsAliquot:        tax?.cofinsAliquot        ?? 0,
       cofinsCode:           tax?.cofinsCode           ?? '',
       inssAliquot:          tax?.inssAliquot          ?? 0,
+      inssCode:             tax?.inssCode             ?? '',
       csllAliquot:          tax?.csllAliquot          ?? 0,
+      csllCode:             tax?.csllCode             ?? '',
+      issAliquot:           tax?.issAliquot           ?? 0,
+      issCode:              tax?.issCode              ?? '',
       ibsAliquot:           tax?.ibsAliquot           ?? 0,
+      ibsCode:              tax?.ibsCode              ?? '',
       cbsAliquot:           tax?.cbsAliquot           ?? 0,
+      cbsCode:              tax?.cbsCode              ?? '',
       serviceName:          tax?.serviceName          ?? '',
       serviceDescription:   tax?.serviceDescription   ?? '',
       serviceExternalCode:  tax?.serviceExternalCode  ?? '',
@@ -410,6 +424,7 @@ export class StakeholderDetailModalComponent implements OnChanges {
         serviceTitle:         v.serviceTitle         ?? '',
         operationNature:      v.operationNature      ?? '',
         totalRetentions:      v.totalRetentions      ?? 0,
+        manualAliquots:       !!v.manualAliquots,
         irfAliquot:           v.irfAliquot           ?? 0,
         irfCode:              v.irfCode              ?? '',
         pisAliquot:           v.pisAliquot           ?? 0,
@@ -419,9 +434,15 @@ export class StakeholderDetailModalComponent implements OnChanges {
         cofinsAliquot:        v.cofinsAliquot        ?? 0,
         cofinsCode:           v.cofinsCode           ?? '',
         inssAliquot:          v.inssAliquot          ?? 0,
+        inssCode:             v.inssCode             ?? '',
         csllAliquot:          v.csllAliquot          ?? 0,
+        csllCode:             v.csllCode             ?? '',
+        issAliquot:           v.issAliquot           ?? 0,
+        issCode:              v.issCode              ?? '',
         ibsAliquot:           v.ibsAliquot           ?? 0,
+        ibsCode:              v.ibsCode              ?? '',
         cbsAliquot:           v.cbsAliquot           ?? 0,
+        cbsCode:              v.cbsCode              ?? '',
         serviceName:          v.serviceName          ?? '',
         serviceDescription:   v.serviceDescription   ?? '',
         serviceExternalCode:  v.serviceExternalCode  ?? '',
diff --git a/src/app/features/stakeholders/stakeholders.mapper.ts b/src/app/features/stakeholders/stakeholders.mapper.ts
index 61439b3..615ca17 100644
--- a/src/app/features/stakeholders/stakeholders.mapper.ts
+++ b/src/app/features/stakeholders/stakeholders.mapper.ts
@@ -173,6 +173,7 @@ export function mapFormToPayload(formValue: any): StakeholderPayload {
       serviceTitle:        s3.serviceTitle?.trim()       ?? '',
       operationNature:     s3.searchNatureOp?.trim()     ?? '',
       totalRetentions:     Number(s3.totalRetentions)   || 0,
+      manualAliquots:      !!s3.manualAliquots,
       irfAliquot:          Number(s3.aliqIRRF)          || 0,
       irfCode:             s3.codIRRF?.trim()            ?? '',
       pisAliquot:          Number(s3.aliqPIS)            || 0,
@@ -182,9 +183,15 @@ export function mapFormToPayload(formValue: any): StakeholderPayload {
       cofinsAliquot:       Number(s3.aliqCOFINS)         || 0,
       cofinsCode:          s3.codCOFINS?.trim()           ?? '',
       inssAliquot:         Number(s3.aliqINSS)           || 0,
+      inssCode:            s3.codINSS?.trim()             ?? '',
       csllAliquot:         Number(s3.aliqCSLL)           || 0,
+      csllCode:            s3.codCSLL?.trim()             ?? '',
+      issAliquot:          Number(s3.aliqISS)            || 0,
+      issCode:             s3.codISS?.trim()              ?? '',
       ibsAliquot:          Number(s3.aliqIBS)            || 0,
+      ibsCode:             s3.codIBS?.trim()              ?? '',
       cbsAliquot:          Number(s3.aliqCBS)            || 0,
+      cbsCode:             s3.codCBS?.trim()              ?? '',
       serviceName:         s3.serviceName?.trim()        ?? '',
       serviceDescription:  s3.serviceDesc?.trim()        ?? '',
       serviceExternalCode: s3.serviceExtCode?.trim()     ?? '',
diff --git a/src/app/features/stakeholders/stakeholders.model.ts b/src/app/features/stakeholders/stakeholders.model.ts
index ef68fb8..d7ff1e7 100644
--- a/src/app/features/stakeholders/stakeholders.model.ts
+++ b/src/app/features/stakeholders/stakeholders.model.ts
@@ -82,6 +82,7 @@ export interface StakeholderTaxesAndServices {
   serviceTitle:         string;
   operationNature:      string;
   totalRetentions:      number;
+  manualAliquots?:      boolean;   // FE-S5: alíquotas manuais (contrato novo)
   irfAliquot:           number;
   irfCode:              string;
   pisAliquot:           number;
@@ -91,9 +92,15 @@ export interface StakeholderTaxesAndServices {
   cofinsAliquot:        number;
   cofinsCode:           string;
   inssAliquot:          number;
+  inssCode?:            string;    // FE-S5
   csllAliquot:          number;
+  csllCode?:            string;    // FE-S5
+  issAliquot?:          number;    // FE-S5 (ISS não existia no contrato antigo)
+  issCode?:             string;    // FE-S5
   ibsAliquot:           number;
+  ibsCode?:             string;    // FE-S5
   cbsAliquot:           number;
+  cbsCode?:             string;    // FE-S5
   serviceName:          string;
   serviceDescription:   string;
   serviceExternalCode:  string;
